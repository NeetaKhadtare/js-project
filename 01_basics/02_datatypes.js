"use strict";   // treat all JS code as newer version
// if we are not uses the "use strict" now days it default works

//alert("Hello")

/* alert(3+3)   //we canot use it in Node.js because it can be used in browser */

console.log(3+3)

console.log("Neeta")

/* write code in the readiablity form donot do follwing 
console.log(3+
    3)
console.log(3+3); console.log("Neeta") 
*/

// use documents from ecma (TC39)

let name= "Neeta"
let age= 23
let isLoggedIn=false

/* Datatype in Javascript 
number which have range as 2 to power 53 
bigint   String (we can use '' & " " also)
boolean = true / false
Null= it is datatype and standalone value also (empty )
undefined = not defined vlaue for varibles (not assinged any value)
symbol = find the uniqueness of component  (more related to react )
object= 
*/

console.log(typeof "Neeta");
console.log(typeof age);   

console.log(typeof undefined);  // it is datatype in javaScript which is undefined
console.log(typeof null);   //answer is Object